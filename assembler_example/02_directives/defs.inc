; ============================================================
;  defs.inc — внешние определения, подключаемые через #include
;  (из того же каталога, что и directives.asm)
; ============================================================
        #define LED_PORT 0x02
        LED_ON    EQU 0xFF
        LED_OFF   EQU 0x00
