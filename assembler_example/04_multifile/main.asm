; ============================================================
;  main.asm — главный модуль (многофайловая сборка)
;
;  Импортирует HELPER из helper.asm и вызывает его.
;  Демонстрирует: ORG, IMPORT, EXPORT, метку ::, CALL.
; ============================================================
        ORG 0x0100

        IMPORT HELPER
        EXPORT main

main::
        MVI A, 0x42
        CALL HELPER
        HLT
