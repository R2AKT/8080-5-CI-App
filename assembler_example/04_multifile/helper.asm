; ============================================================
;  helper.asm — вспомогательный модуль (многофайловая сборка)
;
;  Экспортирует HELPER, который вызывается из main.asm.
;  Демонстрирует: ORG, EXPORT, метку ::.
; ============================================================
        ORG 0x0200

        EXPORT HELPER

HELPER::
        ADD B
        RET
